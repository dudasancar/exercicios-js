function verificar() {
    var meuNumero = 3;
    var teuNumero = document.getElementById('num').value;
    if(meuNumero != teuNumero) {
        alert('Tente de novo :(')
    } else {
        alert('Na mosca!!')
    }
}