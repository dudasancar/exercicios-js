function verificar() {
    var frase = document.getElementById('text').value;
    if(frase === 'assim') {
        alert('tem os caracteres ASSIM')
    } else {
        alert('não tem os caracteres')
    }
}